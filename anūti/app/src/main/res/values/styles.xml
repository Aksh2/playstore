<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:itemBackground">@color/white</item>
        <item name="android:actionMenuTextAppearance">@style/MenuText</item>

        <item name="windowActionBar">false</item>
        <item name="android:colorForeground">@android:color/white</item>
    </style>

    <style name="MenuText" >
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">12sp</item>
    </style>
    <style name="AppTheme.Dark" parent="Theme.AppCompat.NoActionBar">
    <item name="colorPrimary">@color/primary</item>
    <item name="colorPrimaryDark">@color/primary_dark</item>
    <item name="colorAccent">@color/accent</item>
        <item name="android:colorBackground">@color/primary</item>
    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat">
        <item name="android:itemBackground">@color/white</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackground</item>

        <!-- Text View Styling-->
        <item name="android:textViewStyle">@style/styleTextView</item>
        <!-- To change the color when edit text is in normal state -->
        <item name="android:colorControlNormal">@color/darkIron</item>
        <!-- To change the color of edit text when activated -->
        <item name="android:colorControlActivated">@color/black</item>

        <!-- Style for the button -->
        <item name="android:buttonStyle">@style/styleButton</item>
        <!-- Style for edit text -->
        <item name="android:editTextStyle">@style/styleEditText</item>
    </style>

    <style name="AppTheme.DarkDialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/white</item>
        <item name="android:textColorPrimary">@color/iron</item>
        <item name="android:background">@color/primary</item>
    </style>



    <style name="styleTextView" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_marginTop">8sp</item>
    </style>

    <style name="styleEditText" parent="@android:style/Widget.EditText">
        <item name="android:textColor">@color/black</item>

    </style>

    <style name="styleButton" parent="@android:style/Widget.Button">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:layout_marginTop">70dp</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
